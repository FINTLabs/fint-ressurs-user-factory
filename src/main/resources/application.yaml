fint:
  application-id: fint-ressurs-user-factory
  kafka:
    topic:
      org-id: ${fint.org-id}
      domain-context: ressurs
    application-id: ${fint.application-id}
spring:
  kafka:
    consumer:
      group-id: ${fint.application-id}
  profiles:
    include:
      - observability
logging:
  level:
    root: INFO
    org.apache.kafka: INFO